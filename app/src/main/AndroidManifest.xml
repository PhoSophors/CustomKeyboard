<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31" >

        <activity
            android:name=".activity.RegisterActivity"
            android:theme="@style/Theme.Material3.Light.NoActionBar.RegisterLogin" />
        <activity android:name=".activity.UpdateProfileActivity" />
        <activity
            android:name=".activity.WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Material3.Light.NoActionBar.Welcome" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity -->
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.Material3.Light.NoActionBar.Profile" /> <!-- RegisterActivity -->
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/Theme.Material3.Light.NoActionBar.RegisterLogin" /> <!-- ProfileActivity -->
        <activity
            android:name=".activity.ProfileActivity"
            android:theme="@style/Theme.Material3.Light.NoActionBar.Profile" />
        <activity
            android:name=".activity.WelcomeCreateAccountActivity"
            android:theme="@style/Theme.Material3.Light.NoActionBar.ActivityLightBackground" />
    </application>

</manifest>

